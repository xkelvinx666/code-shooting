import ImageButton from '../image-button';

export default function ChangeStatusButtons() {
  return (
    <>
      <ImageButton x={20} y={300} image={} onClick={} />
      <ImageButton x={20} y={500} image={} onClick={} />
      <ImageButton x={20} y={700} image={} onClick={} />
    </>
  );
}
